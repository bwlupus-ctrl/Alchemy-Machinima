# BD → Alchemy Merge Patch Log

Tracking log for the Black Dragon → Alchemy feature merge, per the make-ready
work specification (`Make-Ready_BD_to_Alchemy_Feature_Merge.md`). One entry per
work item. Newest entries at the top of each table.

## Conventions (established by A0.1)

- **One item = one revertable commit** on `develop`, message prefixed
  `[BDMerge <item-id>]`. Revert with `git revert <sha>`; no item may depend on
  another item's commit unless its `Depends on` gate says so.
- **Runtime gate:** every item that changes behavior is gated by a debug
  setting in `indra/newview/app_settings/settings.xml`, default **off** unless
  the item spec says otherwise. Setting comment carries the `[BDMerge <id>]`
  tag. Per-frame reads use `LLCachedControl`.
- **Compile-time gate (only when needed):** `LL_BDMERGE_<ITEM>` define in
  `indra/newview/CMakeLists.txt`, defaulting to the feature compiled **in**
  (runtime setting still gates behavior). Use only where a runtime gate is
  impossible (e.g. shader-tree restructuring).
- **Origin notes:** files lifted from a donor keep their license header intact
  (BD headers, or the Firestorm header per
  `FIRESTORM-SOURCE_LICENSE_HEADER.txt` in the donor repo). Blended files get
  a short header note: donor, BD commit/release (or fork commit), and the
  `[BDMerge <id>]` tag. Contributor credits (Tofu Buzzard, Geenz, …) are
  retained in headers and mirrored here.
- **Follow-up fixes** from the donor's changelog land in the same commit as
  the feature (Ground Rule 4).
- **Log entry format:** item id, commit sha, donor + donor ref, gate name(s),
  credits, notes.

## Donors

| Key | Donor | License | Ref |
|-----|-------|---------|-----|
| BD | Black Dragon viewer (NiranV Dean) | LGPL-2.1 (LL-derived) | per-item commit/release |
| FSX | bwlupus-ctrl/phoenix-reshade-XL (Firestorm fork) | LGPL-2.1 | https://github.com/bwlupus-ctrl/phoenix-reshade-XL |

## Items landed

| Item | Commit | Donor | Gate(s) | Credits | Notes |
|------|--------|-------|---------|---------|-------|
| A5.4-1a — velocity / motion-vector buffer (Phase 1a: rigid + camera) | (this commit) | BD (velocity subsystem; NiranV Dean) | `BDMergeVelocityBuffer` (Bool, default **off** = byte-identical stock, no extra pass) + `BDMergeVelocityDebug` (Bool, default off) + `BDMergeMotionBlurStrength` (S32 32) | NiranV Dean (Black Dragon) | Foundation for ghost-free SMAA T2x (Phase 2) and motion blur (Phase 3). Adds a `GL_RG16F` velocity RT (`mVelocityMap`, shares deferred-screen depth), a `renderGeomVelocity()` geometry pass (invoked in `renderDeferredLighting` after post-deferred geometry, before the last-frame matrix snapshot, `!gCubeSnapshot`), four per-pool virtual hooks on `LLDrawPool` (`getNumVelocityPasses`/`begin`/`render`/`endVelocityPass`; base 0-pass) implemented **rigid-only** for Simple/Grass/Fullbright/AlphaMask/FullbrightAlphaMask/Bump(+Shiny)/Materials(12)/GLTF-PBR/Terrain/Tree with explicit Phase-1b rigged seams, and `LLRenderPass::pushVelocityBatches`/`pushVelocityBatchesTextured` (rigid, per-object prev-matrix write-back) + `bindVelocityUniforms`. Prev-state: new `LLDrawInfo::mLastModelMatrix` ← `LLDrawable::mLastVelocityMatrix`, wired in `registerFace`; camera prev view-proj reuses existing `gGLLastModelView`. Shaders `velocityV/F`, `velocityAlphaV/F`, `velocityDebugF` (`class1/deferred/`); programs `gVelocity{,Alpha,Debug}Program`; new reserved uniforms `CURRENT_MODELVIEW_MATRIX`/`LAST_MODELVIEW_MATRIX`/`LAST_OBJECT_MATRIX`/`PROJECTION_MATRIX_UNJITTERED`/`DEFERRED_VELOCITY`/`MOTION_BLUR_STRENGTH`. **Un-jittered velocity (brief pitfall 1):** `llviewercamera` captures the pre-T2x-jitter projection into `gPipeline.mVelocityProjMat`; shaders raster `gl_Position` with the jittered MVP (aligns with the jittered colour/depth buffer) but compute the motion vector from the un-jittered projection for both current & previous → the ±0.25px jitter cancels, no ~0.5px reprojection wobble. Debug viz blits `mVelocityMap` in `renderFinalize` (right→red, up→green, static→grey). Toggling the buffer reallocates RTs (`handleReleaseGLBufferChanged`). Scope notes: PBR/material alpha-mask use plain (opaque) velocity → slight silhouette over-cover, acceptable for the foundation; skinned/rigged velocity, `AVATAR_LAST_MATRIX`, and the `HAS_SKIN` shader branch are the Phase 1b seam; Phase 2 will bind `mVelocityMap` in `resolveSMAAT2x` and also allocate it when T2x is active. **Owed (no interactive SL session):** enable both settings, pan camera → plausible RG velocity on rigid objects + terrain; hold still → ~neutral grey; OFF = unchanged image / no extra pass. |
| A5.4-1b — rigged/skinned + classic-avatar velocity (Phase 1b) | (this commit) | BD (velocity subsystem; NiranV Dean) | same gate: `BDMergeVelocityBuffer` (Bool, default **off** = velocity pass never runs = byte-identical) | NiranV Dean (Black Dragon) | Fills the Phase-1b seams: avatars now stamp TRUE limb velocity instead of camera-fallback motion, closing the biggest remaining input to temporal ghosting (SMAA T2x history, ReShade TAAU/RTGI reprojection). Prev-frame mesh-rig palettes via `MatrixPaletteCache.mLastGLMp`/`mLastFrame` (swap-stash in `updateSkinInfoMatrixPalette`, allocation-free); new reserved uniform `AVATAR_LAST_MATRIX` ("lastMatrixPalette", lockstep); `getLastObjectSkinnedTransform()` in `avatar/objectSkinV.glsl` (DCE'd everywhere else); `LLRenderPass::uploadVelocityMatrixPalettes` uploads current+previous per (avatar,mesh) key — **hardened over donor:** previous palette falls back to current (zero limb velocity) when non-contiguous (`mLastFrame != gFrameCount-1`) or size-changed, and can never leak across avatars; `pushRiggedVelocityBatches{,Textured}` wired into every pool velocity hook (Simple/Fullbright/AlphaMask/FullbrightAlphaMask/Bump/Shiny/FullbrightShiny/Materials×12/GLTF-PBR, each `+1` rigged type); programs `gVelocitySkinnedProgram`/`gVelocityAlphaSkinnedProgram` (make_rigged_variant) + `gAvatarVelocityProgram` (classic; NEW `avatarVelocityV.glsl`, reuses `velocityF.glsl`, un-jittered convention, row-vector palette apply verified against `avatarV.glsl`); classic prev palette `LLViewerJointMesh::mLastMatrixPalette[45*4]` + avatar-pool velocity hooks (impostor/too-slow/invisible guarded). **Owed in-world:** debug viz — animated avatar under a locked camera paints RG on limbs (pre-1b: flat grey); no trail behind a walking avatar; impostors stay fallback-grey; OFF = stock. |
| Batch 4 — Machinima High-LOD / anti-cull filming mode | (this commit) | novel (NET-NEW; brief confirmed nothing to port from BD) | `BDMergeMachinimaHighLOD` (Boolean, default **off** = byte-identical stock) | — | Master filming toggle implementing all four levers from `doc/BDMERGE_LOD_ZOOM_BRIEF.md`. **(1) Force object LOD 3** — new `LLVOVolume::sMachinimaForceMaxLOD` static (`llvovolume.h`/`.cpp`); `calcLOD()` gains a `sMachinimaForceMaxLOD` branch ahead of the HUD-force site (`llvovolume.cpp` ~`:1577`) forcing `cur_detail = 3` for every volume → kills wide-shot LOD loss AND the LOD-change popping (cur_detail stops changing → `updateLOD`'s `markRebuild` stops firing). This is the core. **(2) Uncap LOD factor** — new `MACHINIMA_MAX_LOD_FACTOR = 32.0f` (`llvovolume.h`); both clamp sites (`handleVolumeLODChanged` in `llviewercontrol.cpp`, startup in `llappviewer.cpp`) now clamp against the raised ceiling only while the mode is on (stock stays 4.0). **sDistanceFactor sign fix** — both sites now `llmax(0.1f, 1 - sLODFactor*0.1f)` so the distance term can't go negative past factor ~10. Lightbox Object LOD slider/spinner max_val 4.0 → 32.0. **(3) Kill avatar impostors** — done by *gating the reads*, not writing settings (no clobber of the user's saved values): `LLVOAvatar::isImpostor()`/`shouldImpostor()` skip the rank/count impostor path when the mode is on; `isTooComplex()` auto-branch returns false (disables the complexity + `RenderAutoMuteSurfaceAreaLimit` auto-mute) — manual mutes (AV_DO_NOT_RENDER / mute list) still apply. Turning the mode off simply stops gating → prior behavior returns. **(4) Relax small-face cull constants + raise reach** — `FORCE_SIMPLE_RENDER_AREA`/`FORCE_CULL_AREA` now read through `machinimaForceSimpleRenderArea()`/`machinimaForceCullArea()` accessors returning 0 while the mode is on (small/distant faces keep full materials, aren't dropped from batching) at all 3 usage sites; Lightbox Draw Distance (`RenderFarClip`) UI max 1024 → 2048 for vistas. **Guardrails honored:** frustum + occlusion culling untouched (never disabled). UI: "Machinima High LOD" checkbox in Lightbox ▸ LOD section with VRAM/tri-spike + enable-before-rolling tooltip. Startup honors a persisted-ON toggle (`llappviewer.cpp`). **Owed (no interactive SL session):** toggle on, pull camera back on a detailed scene → detail holds / no popping; avatars stay 3D at distance; toggle off → prior LOD + impostor behavior returns; confirm frustum+occlusion still culling; watch VRAM/triangle spike on dense regions. |
| B8s — camera straight-lift (verbatim-BD fidelity corrections; supersedes reinterpreted bits of B6/B8/B12) | (this commit) | BD | `BDMergeCameraStraightLift` (default off; master gate) + `AllowCameraFlipOnSit` (BD key, default on, only read while the master gate is on) | NiranV Dean (Black Dragon) | **Recon retired the "hard lock" premise (source-verified).** BD's `CameraFollowJoint` is proven focus-follow: it writes only `focus_agent` (the aim point) in `updateCamera` (`llagentcamera.cpp:1528-1535`); camera position comes from `getAvatarRootPosition()` = `mRoot->getWorldPosition()` (`:2206`), never the joint, and `grep` of the whole BD `newview/` tree shows `mFollowJoint`/`CameraFollowJoint` nowhere else. So **the bone camera itself needed NO change** — the current B6 port already matches BD byte-for-byte, and the fork's `llcinematiccamera` `MODE_BONE_LOCK` (true rigid pos+rot GoPro lock, `patternBoneLock`) stays the separate additive tool (untouched). What this commit lands are the 4 genuine BD-fidelity deltas the prior B8/B12 ports diverged on, all behind the default-off master gate so stock/F5 behaviour is bit-identical when off: (1) BD's `mCameraRollAngle = 0.f` reset inside the third-person focus-on-avatar block — mapped to Alchemy's `mRollAngle` (BD reverts accumulated key-roll so it can't fight cinematic head-roll; **note this intentionally zeroes Alchemy's F5 third-person camera roll while the gate is on** — the direct BD↔F5 conflict, resolved BD-wins-under-gate); (2) removed the added `mCameraUpVector.normalize()` (`:1582`) that BD does not have (undoes the B8 reinterpretation); (3) BD's sit-time up-vector flip now fires in BOTH mouselook and third person, guarded by `mFocusOnAvatar` and gated by `AllowCameraFlipOnSit` (BD default 1) — stock Alchemy only flipped in mouselook and never consulted the setting; (4) restructured bone/cinematic to BD's mutually-exclusive `if/else-if` under the single `isAgentAvatarValid && mFocusOnAvatar && THIRD_PERSON` guard that owns the roll reset. Also aligned `CameraMaxRoll`/`CameraMaxRollSitting` defaults 0.9/0.95 → BD's **0.85** (cinematic is opt-in, so blast radius is nil). Not a hard-lock change. Donor: BD `llagentcamera.cpp` `updateCamera` + `settings_blackdragon.xml` (`CameraMaxRoll*`, `AllowCameraFlipOnSit`). In-world verification owed (no interactive SL session): cinematic head-roll, sit-flip in third person, gate-off = stock + F5 roll intact. |
| G3.3 — per-projector volumetric light cones (visible spotlight shafts) ⭐ | (this commit) | novel (NET-NEW; not a BD port) | `BDMergeProjectorVolumetrics` (default off) + `BDMergeProjectorVolumetricsResolution` (16) / `Multiplier` (1.0) / `Anisotropy` (0.72) | built alongside G3.2 (Tofu Buzzard / NiranV Dean godray lineage); local-light march is net-new | Net-new **local-light** companion to the sun-only G3.2. G3.2 raymarches the whole view ray against the sun cascades (parallel rays, constant direction); a projector is a **bounded local cone**, so this is designed for that regime, NOT a clone of the sun march. **Local-light design choices:** (1) **Bounded march** — each cone intersects the view ray with the projector's sphere-of-influence (center, size) analytically and marches ONLY [t0,t1] clamped to the opaque surface, so the whole sample budget lands inside the lit segment (far better quality/sample than the sun path → default just **16** samples vs the sun's 32). (2) **Real projector attenuation** — reuses `clipProjectedLightVars` (frustum clip + dist + l_dist + proj_tc in one call) and `calcLegacyDistanceAttenuation`, identical to how `spotLightF` dims the lit surface. (3) **Cookie/gobo modulation** — `getProjectedLightDiffuseColor(l_dist, proj_tc.xy)` colours+shapes each sample by the projection texture, plus the `proj_tc.xy∈[0,1]` cone-footprint test lifted verbatim from spotLightF. (4) **Per-sample Henyey-Greenstein phase** — because the light is local the scatter angle varies per step; `g` (anisotropy) exposed, default **0.72** forward-scatter (bright glow looking toward a set light). (5) **Volumetric self-shadowing** — `sampleSpotShadow(spos, 0, slot, tc)` per step samples the projector's own shadow map (NSpot shadowMap4-9 / `shadow_matrix[4+idx]`). (6) **Additive over slots** — one fullscreen cone per slot, `glBlendFunc(GL_ONE,GL_ONE)`, each outputs ONLY the shaft delta (no read/write feedback, no extra ping-pong), clamped in-shader (PROJVOL_MAX) for HDR safety; runs in `renderFinalize` right after the sun volumetric block, additively in place on the source buffer, its own independent gate. **R1 (critical):** new **side-effect-free `setupSpotLightVolumetric()`** copies only setupSpotLight's geometry+cookie upload and OMITS the `mTargetShadowSpotLight[]` priority reshuffle (takes the slot as a param) — never call setupSpotLight from finalize. New `class3/deferred/projectorVolumetricF.glsl` (real march; needs deferred shader level 3) + `class1` no-op stub (outputs black, additive-safe); new `gDeferredProjectorVolumetricProgram` (mirrors G3.2 features, `SPOT_SHADOW=1` instead of `SUN_SHADOW`); new reserved uniform `projvol_g` (reuses `godray_res`/`godray_multiplier` for count/brightness). UI in Lightbox ▸ Godrays under the G3.2 controls. **Scope limits:** shadow-casting projectors only (slots 0..N-1; raise `BDMergeMaxSpotShadows` to promote more); full-screen triangle per cone (scissored bounding-quad + spatial downsample are documented follow-ups); reuses G3.2's post-`colorCorrect` display-stage placement. **Owed:** in-world acceptance — shafts visible through a shadowed projector at capture res, gobo-shaped, correct forward-scatter, no HDR blow-out across N=2/4/6; brightness defaults (multiplier/PROJVOL_SCATTER) are conservative starting points and need in-world tuning. |
| B13 — BD EEP environment editor rework | `bf3a94f798` | BD (live HEAD subset) | `BDMergeEnvLocalPresets` (default off; new floaters/toolbar buttons are additive opt-in surfaces) | NiranV Dean (Black Dragon) | **BD's resurrected classic Windlight editors are DEAD in BD HEAD** (llfloatereditsky/water not in BD CMakeLists, headers deleted) — not ported. Ported what's alive: `bdmergeenvlibrary.cpp/h` (env-preset library extracted from bdfunctions: disk presets under `windlight/{skies,water,days}` user+system, EEP `"version"`-keyed + legacy formats, inventory listing, save/delete), `LLFloaterWaterAdjust` (`env_adjust_water`), `LLFloaterEnvironmentSettings` (`env_settings`), llenvironment cloud-scroll axis locks + `createXFromPreset` + `isLocalPreset`, llsettingsvo `buildFromPresetFile` trio, gated local-preset surface in Fixed Environment editor (preset combo, Save To Disk incl. region envs — personal-build ruling, Delete w/ system-preset refusal) + Personal Lighting strip, sky/water/windlight toolbar commands (RLV setenv-gated), 82 BD-only preset files. Dropped: BD water-height fudge (BD-only region API), BD's stock-floater XML rewrite (Alchemy's refactored `llfloatereditenvironmentbase` structure kept, BD behavior applied piecewise). In-world smoke test owed. |
| F3 — FS Extended Radar + Show Friends Only | `c80b457101` | FS | none (opt-in floater; alert/report settings all default off) | Firestorm contributors | Full FSRadar port under donor names (Comm ▸ Radar): distance/time/age columns, chat+draw range enter/leave reports + sounds, radius limiter, name formats, distance coloring, per-avatar + multiselect actions, embedded minimap, Show Friends Only (`FSRadarShowFriendsOnly`, session-only temp-derender of non-friends via ALDerenderList, optional auto-off after TP). **Enabler: ALDerenderList TYPE_AVATAR was dead scaffolding** — added populating ctor/addAvatar()/isDerendered(type,id) + avatar gates at both llviewerobjectlist chokepoints (mirrors TYPE_OBJECT pattern). Same-region enter/leave alerts NOT duplicated (Alchemy's AlchemyRadarAlerts already ships them). Stripped (no Alchemy equivalent): contact sets, keyword alerts, LSL-bridge height, Milkshake toasts, minimap marks, script info. `radarbox_*` icon assets not ported (blank; asset follow-up). Tracker beacon via `trackLocation` stand-in. |
| F4/F8 — Phototools V2 + composition guides | `f33b3af015` | FS (recast thin) | `FSSnapshotDoFWysiwyg` (default ON — snapshot-only effect), `RenderFocusPointCrosshair` / `RenderCompositionGuide*` (default off) | William Weaver "paperwork" (Phototools), Beq Janus (focus point, FIRE-16728/32023) | **Recast, not verbatim:** FS quickprefs' controls mostly already exist (Lightbox Rendering tab, quick settings) — new `ALFloaterPhototools` (World ▸ Phototools) only adds EEP preset quick-switcher + DoF focus tools + guides, binding the rest to existing keys. WYSIWYG DoF scales `DOF_TAN_PIXEL_ANGLE`/`DOF_MAX_COF` by window/target height ratio (composes with A1.2 autoscale — different stages, no double-scale; 1.0 on-screen). Focus crosshair + F8 rule-of-thirds/golden-ratio(4 orientations + spiral)/diagonal overlays draw in UI pass → excluded from show-ui-off snapshots. New settings in `settings_alchemy.xml` (siblings live there). Alt+Shift+X = focus lock. Caveat: guide line width >1px is a no-op on core-profile GL. FS capture-frame border shader + quickprefs tray editor deliberately not ported. |
| G4.1 — per-cascade + projector shadow resolution | `5b56a25d5a` | BD behavior (Vector4 per-cascade res), novel wiring | `BDMergeShadowResolution0-3` + `BDMergeProjectorShadowResolution` (all 0 = stock) | NiranV Dean (behavior) | Stock ties all 4 sun cascades to screen-res × `RenderShadowResolutionScale` (non-square, uniform); override gives each cascade a custom square size + independent projector maps. Wired to `handleShadowsResized` → live realloc, no recompile. Lightbox ▸ Shadows spinners added. Caveat: `shadow_res` uniform reflects cascade 0 → PCF offsets proportionally scaled on differently-sized cascades (donor parity; revisit in G1.3). On the 5090: 4096/4096/2048/2048 is a sane machinima start. |
| G3.2 — volumetric lighting / godrays ⭐ | `405baf77bf` | BD (v5.6.3 tree; Tofu Buzzard lineage, NiranV Dean rework) | `RenderVolumetricLighting` (default off) + `Directional` (shader-compile-time fade) / `Resolution` (32) / `Multiplier` (1.0) / `FalloffMultiplier` (1.0) | Tofu Buzzard (original scattering), NiranV Dean (Black Dragon) | Donor ALIVE post-PBR (unlike A1.1/G2.3): real raymarch at BD `class3/deferred/volumetricLightF.glsl`, class1 = passthrough stub for low shader levels (note: needs deferred shader level 3 — feature-table dependent). Ported both shaders + `nonpcfShadow`/`nonpcfShadowAtPos` into `shadowUtil.glsl` + 3 reserved uniforms + program + `renderVolumetric()` pass. Placement: renderFinalize after glow-generate, before FXAA/SMAA and glow-combine (spec ordering constraints both satisfied by Alchemy's chain shape). Shadow-gated (`RenderShadowDetail > 0`) — raymarch samples sun cascades. **Two donor bugs fixed forward:** GODRAYS_FADE permutation was applied to BD's soften program (dead define — the #if lives in the volumetric shader); BD renders src→src while sampling the same target (UB feedback) — ported as proper ping-pong. In-world acceptance owed: shafts through shadowed opening at capture res, FXAA intact at high brightness, glow present. HDR-path note: pass runs in both HDR and legacy paths (glow combine only exists in legacy; ordering constraint moot in HDR). |
| G5.4 — parallel texture-cache body I/O | `876f6597ab` | novel (spike-justified) | `BDMergeCacheIOThreads` (default 4; 0 = stock single-threaded) | — | Spike session 3: cache reads 781ms mean / 5s p95 on NVMe (9931 summed s vs 958s decode) — the texture cache is one `LLWorkerThread` doing sync file I/O inline. BODY read/write stages now post to `LL::ThreadPool("CacheIO")` and re-poll (fetch-worker pattern). Pool threads touch only a self-owned job object (reads adopt buffer, writes get a copy); ALL header-file + entry bookkeeping stays on the TextureCache thread (serialization unchanged, aborts can't race — job completes into the void). **Diagnostic correction logged: `LLLFSThread` is NOT the texture cache's I/O path** (vestigial — handles never assigned); it serves audio decode only. Stock sync path preserved verbatim at setting 0. Validation owed: next session's spike report cache_read row. |
| G5.1-2b-tiers — tiered pool residency | `24c97a2fda` | novel (brief B-2b.1) | `BDMergeTexPoolRegionGraceTTL` (900s) + `BDMergeTexPoolRecencyWindow` (300s) | — | Eviction = three oldest-first passes: departed-region entries past grace TTL → idle entries past recency window → plain LRU backstop. Return-TP within TTL finds region textures warm. Entries stamp last-hit + region handle (atomic mirrored main-thread). Pool API/hooks unchanged. |
| G2.3 — forced client-side alpha masking | (this commit) | novel (BD donor code no longer exists — same PBR-refactor loss as A1.1; implemented per spec behavior) | `BDMergeForceAlphaMask` (default off) + `BDMergeForceAlphaMaskCutoff` (0.5) + `BDMergeForceAlphaMaskRigged` (default off) | — | Single-predicate design: `LLFace::canRenderAsMask()` (`llface.cpp`) is the sole router into PASS_ALPHA_MASK / PASS_FULLBRIGHT_ALPHA_MASK (both `llvovolume.cpp` sites + shadow passes follow batch membership automatically; `sNoAlpha` precedent proves unconditional-true is supported). Forced branch overrides creator-chosen BLEND and texture heuristics; faces with transparency %% (color alpha < 1) or glow keep blending (masking them would misrender). Cutoff: mask batches bind per-batch `mAlphaMaskCutoff` (`lldrawpool.cpp:529,555` incl. the rigged variant), so the global slider lands at the two `genDrawInfo` assignment sites — forced-BLEND-material faces get the global cutoff (their material cutoff is meaningless, often 0), and while forced mode is on the whole masked set follows the slider (stock 0.33/0.5 constants otherwise). Rigged faces excluded by default (stock policy; `pushRiggedMaskBatches` exists so the opt-in setting rides a real path — expect visible changes on worn alpha hair/clothing, per-shot call). **Scope cut: legacy Blinn-Phong only** — PBR/GLTF blend faces route by `mAlphaMode` before `canRenderAsMask()` is consulted (`te->getGLTFRenderMaterial()` early-out kept); PBR forcing needs the GLTF material sites + `mAlphaCutoff` override, briefed as follow-up. Spec gotchas: emissive-mask cutoff regression + in-world z-fight verification owed at next session. Landed without G2.2 gate (user directive; gate was shared-region sequencing, not a hard dependency). |
| G5.2 — capture-mode pin | `7c89378321` | novel | `BDMergeCaptureModePin` (default **off** — disables a memory safety valve by design) | — | Holds `sDesiredDiscardBias` at floor (1.0) at the single choke point in `LLViewerTexture::updateClass` (before the final clamp) — neutralizes every bias consumer at once: per-texture downrez in `updateImageDecodePriority`, off-screen unload thresholds (`BIAS_TRS_*`), the backgrounded-viewer bias-5 dump (alt-tab during a recording no longer dumps textures), and the bias>2 draw-distance shrink (`llviewerdisplay.cpp:225`). Budget half of the spec item was already landed by G5.0; G5.1 pools make any refill instant. Note: its settings.xml block rode along in commit `6d6fdf169d` (tooling slip — code declares its own default, so reverting either commit independently stays safe). Optional pre-take prefetch = G5.3 (briefed). |
| G5.1-2b — decoded-mesh RAM pool | `6d6fdf169d` | novel | `BDMergeMeshPoolEnable` (default on) + `BDMergeMeshPoolFraction` (0.1) / `MaxMB` (0=auto) / `FloorMB` (256) | — | Mesh twin of the texture pool: `bdmergemeshpool.cpp/h`, keyed (mesh id, LOD), entries are exclusively-owned deep copies via `LLVolume::copyVolumeFaces` (consumers mutate volumes — `cacheOptimize`, rigging — so refs never cross the pool boundary). Serve: `fetchMeshLOD` post-header-unlock, pre-disk-read — hit replicates `lodReceived`'s post-unpack delivery (skin-map lookup + `updateRiggingInfo` re-run against current avatar state + `mLoadedQ`). Write-through: `lodReceived` inserts pre-rigging copy after successful unpack. Encoded disk mesh cache untouched. Settings refresh on main thread in `notifyLoadedMeshes`. Byte accounting estimates face arrays (pos/norm/tc + tangents/weights when present + indices). Stats line in the spike report. Caveat: physics shapes and skin/header fetches are NOT pooled (different pipelines; briefed for Stage 2b if measurements warrant). |
| G5.1 4a-i — ImageDecode thread-pool ceiling | (this commit) | novel (spike-justified) | `BDMergeDecodeThreadCeiling` (default 24; 0 = stock 16) | — | Report #1 measured ~45 threads' worth of queued decode demand (14287 summed decode-seconds / 319s window; p50 20ms, p95 5s) against the stock `llclamp(cores−6, 2, 16)` pool. New setting replaces only the ceiling; formula unchanged. On the 9950X (32 logical): stock → 16 threads, default 24 → min(26, 24) = 24. Stock code rewrites `ThreadPoolSizes["ImageDecode"]` every launch, so a code-level ceiling was required (settings-only override impossible). Applies at next launch. |
| G5.1 Stage 2 (core) — decoded-texture RAM pool | `6fbf94c6e6` | novel (net-new per Analysis Finding 5) | `BDMergeTexPoolEnable` (default on) + `BDMergeTexPoolFraction` (0.5) / `BDMergeTexPoolMaxMB` (0=auto) / `BDMergeTexPoolFloorMB` (1024) | — | `bdmergetexpool.cpp/h`: UUID-keyed pool of decoded pixel copies, LRU-evicted against a budget auto-sized from `gSysMemory.getPhysicalMemoryKB()` × fraction (→ ~96GB on the 192GB target; manual override; no hardcoded GB figures). Read path: fetch-worker INIT state checks the pool before the encoded-cache read — hit at desired-or-finer discard sets raw/discard/DONE, skipping disk read AND J2K decode (`lltexturefetch.cpp` INIT block). Write-through: successful decode inserts a copy before WRITE_TO_CACHE, so the encoded disk cache persists everything and the pool is never source of truth. Entries own their bytes (no LLImageRaw ref sharing across threads — immune to consumer mutation and non-atomic refcounts); copies run on fetch worker threads, never the render thread. Settings mirrored to atomics on the main thread in `createRequest` (LLCachedControl isn't thread-safe); gate-off drops all entries (off = stock footprint). Excluded: aux-needing and non-FTT_DEFAULT (bakes) requests. Pool stats line appended to the G5.1-S1 spike report. **Deliberate Stage 2 scope cuts:** single best-discard entry per UUID (a finer-than-requested hit uploads more VRAM than stock would — acceptable on 32GB); plain LRU (recency window ≈ implicit) — tiered residency (region grace TTL, pinned locations), non-texture assets, and the dedicated `TexturePool` worker pool are Stage 2b. |
| G5.1-S1 — texture-pipeline measurement spike (instrumentation) | (this commit) | novel | `BDMergeTexSpikeLog` (default **on** — measurement tool, log-only, zero behavior change) + `BDMergeTexSpikeLogInterval` (300s) | — | Stage 1 of G5.1 per spec ("do first, small"). Diff-first found the pipeline already times every fetch stage per request — `mCacheReadTime`/`mDecodeTime`/`mCacheWriteTime`/`mFetchTime` + per-state `mStateTimersMap` (`lltexturefetch.cpp:544-554`), sampled into LLTrace at `getRequestFinished`. Net-new is aggregation + the decision metrics: `bdmergetexspike.cpp/h` accumulates log-scale histograms per stage (mean/p50/p95/max), time-in-state totals (incl. HTTP wait states → network attribution), decode throughput MB/s, GL-upload timing (`LLViewerFetchedTexture::createTexture`, thread-safe — runs on LLImageGL worker), and **repeat-decode tracking** (same UUID decoded again this session = exactly the work the G5.1 decoded pool eliminates; reported as count, %, and seconds spent). Dumps a report block to the viewer log every `BDMergeTexSpikeLogInterval` seconds tagged `[BDMerge G5.1-S1]`. Hooks: one call in `getRequestFinished` (all numbers already in hand there), one timer around `createGLTexture`. Percentiles are bucket-upper-bound approximations (0.5ms–5s log buckets). State-name table in the reporter must track `LLTextureFetchWorker::e_state` order (enum is private; noted in-file). **Data collection owed:** run a normal capture session ≥15 min, then grep the log for `BDMerge G5.1-S1` — decode-dominance (or not) in the last report re-weights G5.1 Stage 2 per spec. |
| G5.0 — memory-detection prerequisites & fast wins | (this commit) | novel (spec-native tuning per Alchemy Memory Analysis 2026-07-07) | `MaxHeapSize64` (0 = auto, 16 = stock), `CacheSize` | Geenz (VRAM budget/divisor design, upstream) | Verified all Analysis findings against this tree first (Ground Rule 6). **Finding 1 (VRAM divisor) already resolved upstream** — Alchemy set `RenderTextureVRAMDivisor` default to 1 in `695247539b` ("mitigate frame stuttering"); Analysis read the in-code fallback (2), settings.xml overrides it. No port (Ground Rule 3); existing `target = min(budget−512, budget×0.8)` (`llviewertexture.cpp:505`) already leaves spec-recommended headroom (~25.6GB target on 32GB). **Finding 6 (heap cap):** `MaxHeapSize64` 0-sentinel auto-sizes to ⅞ of installed RAM, floored at the legacy 16GB default (→168GB on the 192GB target; no hardcoded GB figures per spec banner); default flipped 16.0→0.0. **Finding 3 (detection):** verified 64-bit clean end-to-end — `ullAvailPhys` → `U64Bytes` (`llsys.cpp:800`), `LLMemoryAdjustKBResult` only adds 1MB, 192GB fits `U32Kilobytes`. The one real distortion WAS the heap cap: `llmemory.cpp:157` clamps reported-available to `cap − RSS`, so a viewer using ~15.5GB reads <512MB "available" → false `isSystemMemoryLow()` → bias slam ×`getSystemMemoryBudgetFactor()` + emergency purge, with ~176GB actually free. Plausibly the direct cause of the observed mid-scene degrade; fixed by the auto-size. **Finding 4 (disk cache):** `CacheSize` default 6144→32768 (= `MAX_CACHE_SIZE` ceiling, `llappviewer.cpp:4484`, not raised — returns beyond it shift to G5.1's decoded pool); prefs spinner already allows 32768. Revert = set `MaxHeapSize64` 16 / `CacheSize` 6144 / `git revert`. **Validation owed in-world (spec G5.0):** memory readout >16GB, `gGLManager.mVRAM` detects full 32GB, capture-session degrade check with GPU memory monitor. |
| C8 — slider precision override | `bd71dde48b` | BD | `BDMergeSliderPrecision` | NiranV Dean (Black Dragon) | Donor: BD 94c47d42de (exact isolated commit; the later BD 949792d79d "overdrive beyond min/max" is a different feature, deliberately NOT ported). Typed text-field commits bypass increment snapping when gated on; thumb-drag/keys/wheel always snap. Gated globally via settings (LLCachedControl on LLUI config group, llbutton.cpp precedent) instead of BD's per-widget XUI param (would be dead complexity — no XML sets it). LLSpinCtrl verified not to snap typed input (no change needed); LLMultiSliderCtrl has the same snap pattern but BD's donor commit doesn't touch it — left for donor parity, legit follow-up if ever needed. |
| B3 — Freeze World | (this commit) | BD | `BDMergeFreezeWorld` (+ runtime-only `UseFreezeWorld`, Persist 0) | NiranV Dean (Black Dragon) | Donor: BD b4cfc2cb83 (+ fab3226ac5/4f4f37a4c3/e8cbb4eebd). Avatar pause handles + stock FreezeTime; particles additionally halted in idle() (BD leaves them running; spec requires full scene). Snapshot-floater checkbox occupies the freeze-frame slot when gated on (zero layout slack in advanced panel; C6 owns future floater rework). Guards on every stock FreezeTime-clear path (snapshot updateLayout/dtor/onClose, 360capture) so no half-frozen state. Composes with B3a: pause independent of sGlobalTimeFactor, slow-mo factor survives freeze untouched. Closing the floater always thaws — BD's reopen-refreeze memory deliberately dropped (their bug a1ddcd69a9). Known minor: llscenemonitor also writes FreezeTime (obscure dev tool, unguarded, recoverable by retoggle). |
| B11 — hide lookAt/focus broadcast | `09fef181f3` | BD | `BDMergeHideLookAt` | Kyler Eastridge (Black Dragon) | Donor: BD 82477c484d (feature) + 5785bfdb70 (stuck-head fix: one-time IDLE clear-effect send on enable). Ported only the hide-broadcast half — the limit-distance half (`LimitLookAtTarget`/`LimitLookAtTargetDistance`) is already in Alchemy and MORE elaborate than BD's (per-object exceptions). Deliberately not named `EnableLookAtTarget`: Alchemy already uses that key for an unrelated local-indicator-hide feature. Single choke point: all lookAt setters funnel through LLHUDEffectLookAt::setLookAt(). |
| A1.2 — resolution-aware autoscale (SSAO/shadow-blur/DoF) | (this commit) | BD | `BDMergeSnapshotAutoscale` (+ `BDMergeSnapshotAutoscaleMultiplier`, F32, Persist 0) | NiranV Dean (Black Dragon) | Donor: BD 8eed1a6768 (+ derivation-site fix 33556578a1, non-persist multiplier fa96df28ee, close-reset intent of 0b37fb26fd applied as multiplier reset in onClose). Multiplier = output/window height, derived in llfloatersnapshot updateResolution while gated on; pipeline.cpp reads through a single helper that forces 1.0 when gated off (stricter than BD — no stale scaling possible). Scales: DoF FOV input (renderFinalize), SSAO radius/max-radius (bindDeferredShader), shared shadow+SSAO soften blur size (renderDeferredLighting — Alchemy merged those into one pass post-BD). NOT covered: SSR and volumetric/godrays (donor never touched them; manual reconfig still needed). No floater checkbox — debug-setting gate only; trivially wireable later via control_name. |
| B12 — mouselook head-bone scaling (experimental) | `27fb878af7` | BD | `BDMergeMouselookHeadScale` | NiranV Dean (Black Dragon) | Donor: BD 605f6a7416 (+ fix tail 5f3cd7dcca/1bdedd1137/6bef60d304/5ee5e20dfc/f3196e7aa7/771c862a8a/518b3bbca0). Clean-room reimplementation in llvoavatar (BD drives it from its Poser subsystem, absent here): per-frame apply/restore in idleUpdate (self only), depth-first walk from mHead incl. bento tree. Fixes BD's own getJoint("HEAD") bug (resolves to the collision volume — BD never actually scales mHead). Restore = per-joint default-scale snap-back, not resetSkeleton (sidesteps BD's drift-bug tail); computeBodySize guarded at 3 call sites while scaled + recomputed once on restore (fix-forward of BD 518b3bbca0 and of the lost-recompute defect caught in coordinator review). Known edge: CinematicCamJoint pointed at a descendant of mHead collapses toward mHead's origin while active in mouselook. |
| C3 — camera QoL (wheel modifiers, mouselook offset, numeric fields) | `8e110ced0f` | novel (no BD donor exists for any sub-feature) | `BDMergeCameraQoL` (+ `BDMergeMouselookOffsetX/Y/Z`, F32 m) | — | Diff-first: Shift/Ctrl+wheel height+pitch already in stock Alchemy (handleScrollWheel, upstream commit dd72a05cf14 — mapping is mirrored vs the spec's wording; stock kept, no remap) and numeric fields beside view-angle/distance sliders already present (can_edit_text sliders + advanced-view spinners on the same control_name; synergizes with C8's precision gate). Net-new: X/Y/Z mouselook offset, additive to stock head_offset in calcCameraPositionTargetGlobal's mouselook branch, rotates with the avatar; 3 F32 keys so prefs spinners bind via control_name with zero new C++. UI on Move ▸ Mouse Input tab, enabled_control-bound to the gate. |
| C2 — movement convenience flags | `de36febb9f` | Catznip via BD | `BDMergeMovementFlags` | Catznip (CATZ-400) | Partial-port by design: tap-tap-hold-run (`AllowTapTapHoldRun`) and always-run parity verified already present in Alchemy — closed, no port. Nimble ported both halves: jump-input side (llagent.cpp moveUp adds AGENT_CONTROL_FINISH_ANIM, no AgentUpdate semantics change per PR-1) + landing side (llviewermessage.cpp process_avatar_animation skips PRE_JUMP/LAND/MEDIUM_LAND/STANDUP for own avatar only, sends finish-anim). Donor's two keys collapsed into one gate (coordinator ruling). EXT-2781 fly-hack replicated inside the skip path so gating on can't regress the stuck-fly bug. Other avatars' animations untouched in both gate states. |
| A5.6 — independent light-source toggles | `f84860fa50` | BD | `BDMergeLightToggles` (+ 4 per-class keys, default ON) | NiranV Dean (Black Dragon) | Donor: BD pipeline.h/cpp sRenderOwnAttachedLights/sRenderOtherAttachedLights/sRenderDeferredLights (the last is BD's world-light gate despite the misleading name/comment — its UI label is "Render World Lights"). Filters slot in AFTER stock RenderAttachedLights at all 4 gathering/render sites (calcNearbyLights ×2, setupHWLights, renderDeferredLighting). Projector class is novel — BD's RenderSpotLightImages/Reflections keys are dead (wiring commented out); implemented as fallback-to-omni via existing isLightSpotlight() branch points. LLCachedControl locals instead of BD's statics+listeners (hot-path convention). |
| C6 — snapshot floater conveniences | `92335aa294` | BD | `BDMergeSnapshotExtras` (+ `BDMergeSnapshotRememberMode`/`BDMergeSnapshotLastPanel`/`BDMergeSnapshotResolutionUnlock`/`BDMergeSnapshotAddDateTime`) | NiranV Dean (Black Dragon); LLFloaterBigPreview by Merov (stock LL, resurrected from Alchemy history `44c6630838^`) | Donor: BD fab3226ac5 (detachable big preview) + f0db8914d0 (double-toggle crash fix folded in) + d6c8a45325 (UI-snapshot exclusion, reimplemented as draw() early-return) + 16143bb98b (filename date toggle, default flipped to keep-date so gate-on preserves stock filenames) + 4d0faf18c4 + BD's 12228 res-cap raise (12288 here — BD's value looks like a typo — runtime-gated, save-to-disk panel only, warning alertmodal). Remember-mode extended to cross-session (BD was within-session only; spec asks sessions). BD's leftover FreezeTime-thaw in onClickBigPreview deliberately NOT ported (would clobber B3). BD HEAD itself is broken for this feature (CMakeLists references a deleted file). Gate-off layout bit-identical. |
| B4 — head/eye tracking degree limits | `c8f9fdd86e` | BD | `BDMergeHeadEyeLimits` (+ `BDMergeHeadRotationLimit`/`BDMergeEyeRotationLimit`, degrees) | NiranV Dean (Black Dragon) | Donor: BD 525e398af5 (2019-04-02) + follow-ups 637c2c6f7d/9b824a0b50/29d4267a1e. Replicated BD's behavior, not its wiring: BD reads gSavedSettings inside llcharacter (layering violation); here newview pushes per-frame via LLCachedControl into LLHeadRotMotion/LLEyeMotion statics (sGlobalTimeFactor pattern). Gate-off resets statics to stock constants each frame — bit-identical stock behavior. Gate-on defaults (72°/27°) equal Alchemy's stock constants, not BD's reduced 60°/40°. |
| B2 — camera presets (Z focus/offset, per-preset custom) | `b37c655a69` | BD | `BDMergeCameraPresets` | NiranV Dean (Black Dragon) | Alchemy already ships LL's full XYZ offset/focus/smoothing preset system (save/load/delete/per-control reset) — diff-first found only two genuine deltas: BD-style auto-persist-of-live-edits into the active preset, and true in-place rename (neither stock LL nor BD's own UI had rename; added to meet the spec's rename acceptance). BD's hyphen-delete bug verified not to reproduce (Alchemy uses LLURI::escape consistently). Donor ref: BD 152762d400 (2018-12-03) + 4ff6498a7e. |
| A0.2 — depth-buffer baseline characterization | `3a89dbb9cf` | — | n/a (docs + test tool only) | — | `doc/BD_MERGE_DEPTH_BASELINE.md` + `scripts/perf/bdmerge_depth_compare.py` (self-tested against synthetic captures). In-world baseline reference captures owed at next login; procedure in the doc §6. |
| A0.1 — patch loop + build-flag scaffolding | `d1e60e092a` | — | `BDMergeSamplePatch` (no-op sample) | — | Establishes conventions above; sample gated no-op in `LLAppViewer::idle()`. |
| B3a — animation speed / slow-motion (all avatars) | `333e1e57b4` | FSX | `AnimationTimeFactor` via Advanced ▸ Animation Speed (`LLMotionController::sGlobalTimeFactor`) | Firestorm/Phoenix contributors; custom implementation by bwlupus-ctrl | Landed before this log existed, as part of the phoenix-reshade-XL port. Composition with B3 (Freeze World) still owed when B3 lands: freeze fully stops, slow-mo scales when not frozen, Poser (PR-2) overrides both. |

## Partial / related pre-existing work (not spec items, affects scoping)

Commit `333e1e57b4` (phoenix-reshade-XL port) also landed:

- **Cinematic Camera** (`llcinematiccamera.cpp/h`): bone-lock (GoPro) mode +
  Orbit / Fly Hover / Sweep / Crane patterns. Overlaps **B6 (Bone Camera)**
  and **B8 (Cinematic camera mode)** — when executing B6/B8, diff against
  this first and extend rather than duplicate; BD behavior wins on conflicts
  per PR-0.
- **Flycam Camera Operator** (`llcameraoperator.cpp/h`): procedural handheld
  camera. Adjacent to **B9 (gamepad-native flycam)** — B9 is about input
  mapping/deadzone/smoothing (PR-3, BD authoritative) and must compose with,
  not replace, the operator.
- **ReShade bridge scaffold** (`llreshadebridge.cpp/h`, behind
  `LL_RESHADE_ADDON=0`): outside the merge spec; render items (A-series)
  should avoid breaking its post-`renderFinalize()` capture point.

## A1.1 recon finding (2026-07-09) — donor code no longer exists

Ground Rule 6 verification against BD master (v5.6.3, `I:\black-dragon`)
found that **BD's separable shader loading was dismantled during BD's PBR
refactor.** Current BD's `llviewershadermgr.cpp` and its `setShaders()` path
are structurally identical to Alchemy's monolithic LL baseline; SSAO/shadow
toggles in current BD trigger the same full recompile Alchemy does. What
survives in BD:

- Dead, commented-out granular handlers (`handleSSAOChanged`,
  `handleSSRChanged`, …) at BD `llviewercontrol.cpp:933-976`, calling
  per-feature loaders (`loadShadersSSAO` etc.) that were deleted.
- Two live, small recompile-avoidance wins that DO port directly:
  `handleShadowMapsChanged` (BD `llviewercontrol.cpp:919` — shadow-resolution
  changes do `allocateShadowBuffer()` realloc instead of full recompile) and
  `handleRenderDeferredLightsChanged` (`:750`, flag-only).
- Historical design reference: anchor commit `16bcf38b9c` ("reload shaders
  only where necessary"), follow-up fixes `a9a8d6b3ad`, `7c0b4ce307`,
  `369109abd2`, `d7dd2f38cc` et al.; dismantled in `8b9bc67a72` + PBR merges.

**Ruling (user, 2026-07-09): descope — closed with no code change.**
Follow-up diff showed even the two "portable" handlers don't port standalone:
`handleShadowMapsChanged` reads BD's Vector4 per-cascade
`RenderShadowResolution` (a constituent of **A4.1**, setting absent in
Alchemy), and `handleRenderDeferredLightsChanged` services BD's
light-toggle flags (constituent of **A5.6**, `sRenderDeferredLights` absent
in Alchemy). Both port *with their owning items* (Ground Rule 3 — no
double-application). Alchemy already avoids recompile on shadow-resolution
scaling via `RenderShadowResolutionScale` → `requestResizeShadowTexture()`
(`llviewercontrol.cpp:271,998`). Consequence: items formerly gated on A1.1
(A3.1, A5.x) are ungated; toggle-testing pays a full-recompile stall, same
as current BD.

## Next Fable-block queue (scoped 2026-07-10 night)

**N projector shadows — DONE (`2352f924e0`, 2026-07-11).** Diagnostic (GLSLTextureChannels, un-suppressed this session) refuted the sampler-overflow theory: spot shaders use 7/32 channels. Real cause was the packed screen-space shadow buffer — sunLightF wrote only 2 spot-shadow channels (.b/.a) and spotLightF read them back, hard-capping projectors at 2 (3→lights-without-shadows, 6→blackout from stale-channel reads). Fix: spotLightF now calls `sampleSpotShadow()` to sample each projector's own shadow map directly (all the C++/binding/matrix machinery was already extended to N; only the consumer shader lagged). One-shader change, no C++, no exe rebuild. In-world test of 3/4/6 owed. Original scoping (now historical):

1. **N projector shadows** (`BDMergeMaxSpotShadows`, 2=stock, target 4-6): mSpotShadow/mShadowSpotLight/mTargetShadowSpotLight/mSpotLightFade arrays 2→N; generateSunShadow spot loop; shadow_matrix[6]→[4+N] in EVERY deferred shader incl. shadowUtil + volumetricLightF; shadowMap6+ samplers + llshadermgr reserved uniforms; setupSpotLight slot logic (compose with BDMergeStableSpotShadows priority); bindDeferredShader hardcoded 6-matrix upload (pipeline.cpp:9389 region). One focused session; perf trivial on the 5090.
2. **A5.4 motion blur** (see item row: velocity-pass subsystem).
3. **B9 gamepad flycam** + BD camera accessories (free-DoF focus, mouse-position aim).
4. **NEW — Per-projector volumetric light cones (visible spotlight shafts) — DONE (G3.3, this commit; in-world acceptance owed).** See the G3.3 landed-table row for the delivered local-light design (bounded ray↔sphere march, HG phase, per-slot self-shadowing, side-effect-free `setupSpotLightVolumetric`). Original scoping (now historical): NOT a BD port — BD's volumetric lighting (Tofu Buzzard lineage, = our G3.2 `RenderVolumetricLighting`) is **sun-only** (`volumetricLightF.glsl` raymarches `sun_dir`/`sunlight_color` via `nonpcfShadowAtPos` on the sun cascades shadowMap0-3). This is net-new: extend the raymarch to each shadow-casting projector. Scope: a new pass (or extend `renderVolumetric`) that, per shadowed spot slot 0..N-1, raymarches the view ray *inside that projector's frustum*, sampling its shadow map (shadowMap4-9 / `shadow_matrix[4+idx]` — same machinery NSpot just wired) AND the projector's cookie texture (the projection map, so colored/gobo shafts), accumulating the light's color × falloff. Needs: per-light frustum clip in the march (reuse `setupSpotLight`'s proj_mat/proj_p/proj_n), the spot color/falloff uniforms, additive accumulation over slots (cost scales with N×godray_res — gate + resolution control like G3.2). Composes with the now-fixed per-light shadow sampling. Gated debug setting `BDMergeProjectorVolumetrics` (default off), lives in the Lightbox Godrays tab next to G3.2's controls. Multi-session; real machinima payoff (atmosphere, god-rays from set lights). Prereq met: NSpot done (per-slot shadow map + matrix already bound to shadow-consuming programs).

## Camera directive (user, 2026-07-11, FINAL)

**ALL camera behaviour must be a STRAIGHT LIFT from Black Dragon — no reinterpretation, period.** Overturns the B6 port (CameraFollowJoint was made a "focus-follow, not hard lock" — WRONG). Straight-lift BD `llagentcamera.cpp/h` camera behaviour (bone camera / CameraFollowJoint, head/eye tracking, follow, presets, cinematic) verbatim, adapting ONLY for Alchemy API. **Supersedes the reinterpreted B6/B8/B12.** Fork's home-grown `llcinematiccamera` (bone-lock/patterns) + `llcameraoperator` (VirtualCinema handheld) are non-BD additions — reconcile to BD's behaviour where they overlap/conflict. **RESOLVED (Option 1, user FINAL 2026-07-11) — landed as B8s.** Source verification retired the "hard lock" premise: BD's `CameraFollowJoint` is provably focus-follow (writes only `focus_agent`; camera position is `mRoot`-anchored, never the joint), and the B6 port already matches BD byte-for-byte, so the bone camera was left unchanged and `MODE_BONE_LOCK` stays the separate rigid GoPro tool. B8s lands the 4 real verbatim-BD deltas (roll reset, drop the added up-vector normalize, `AllowCameraFlipOnSit`-gated sit-flip in mouselook+third-person, BD's if/else-if nesting) behind default-off `BDMergeCameraStraightLift`, plus `CameraMaxRoll*` 0.85. Head/eye tracking (B4), presets (B2), follow-cam (stock) confirmed already BD-faithful — no change.

## Item status board

| Item | Status | Blocked by |
|------|--------|-----------|
| A0.1 scaffolding | **done** | — |
| A0.2 depth baseline | **done** (reference captures owed at next in-world session) | — |
| A1.1 separable shaders | **closed — no port** (2026-07-09 ruling: descope; see "A1.1 recon finding") | — |
| A1.2 resolution autoscale | **done** | — |
| A1.3 shadow softening kernel | **closed — donor code gone** (2026-07-10: `blurLightF.glsl` byte-identical BD↔Alchemy; BD's Tofu split-blur kernel dismantled in their PBR sync, same as A1.1/G2.3. Stock parity; novel reimplementation only if stock blur proves insufficient after G4.1 resolution testing) | — |
| A2.1 near-clip reduction | **done** (as G2.1; depth-sanity verification owed with A0.2 captures) | — |
| A2.2 alpha-out-of-depth | open | A0.2 |
| A2.3 forced alpha masking | **done** (as G2.3, landed without A2.2 gate — user directive 2026-07-10; in-world verification owed) | — |
| A2.4 rigged alpha-swap flicker | **closed — already present via shared upstream rewrite** (2026-07-10: BD's fixes 58304bf0d3/8277e334a2/0e4a581def patched the old `LLDrawPoolAvatar::renderRigged` texture-format heuristic that let shadow and main passes classify alpha differently; LL's rigged-rendering rewrite deleted that whole path in BOTH trees — batch membership now drives main+shadow passes from one classification, desync impossible. Only vestigial `sShadowPass` bookkeeping remains. G2.3 forced masking inherits shadow coherence automatically: mask batches carry `mAlphaMaskCutoff` into shadow passes) | — |
| A3.1 decouple SSAO/shadows/SSR | **closed — already present via upstream PBR rewrite** (2026-07-10 recon: sun-lightmap pass gates on `SSAO OR shadows` independently (`pipeline.cpp:9506`), SSAO-only and shadows-only both wired; SSR is Alchemy-native `RenderScreenSpaceReflections`, independent post toggle. BD's decoupling AND the coupling it fixed were both dissolved by the PBR restructure — same pattern as A1.1. In-world every-combination toggle check owed) | — |
| A3.2 volumetric lighting | **done** (as G3.2 ⭐, `405baf77bf`; landed with A2.2 still open — alpha/particle godray participation is the remaining sub-feature, tied to the shared G2.2 toggle; in-world acceptance owed) | A2.2 (alpha participation only) |
| A4.1 per-cascade shadow res | **done** (as G4.1, `5b56a25d5a`; quadratic-sizing sub-feature deliberately skipped — stock scale covers it) | — |
| A4.2 quadratic shadow dims | **closed — donor code gone + superseded by G4.1** (2026-07-11 recon: BD HEAD's shadow allocation is plain square per-cascade sizing from the `RenderShadowResolution` Vector4 (`pipeline.cpp` allocateShadowBuffer, `mV[i]`×`mV[i]` square allocs) — behaviorally identical to G4.1's `BDMergeShadowResolution0-3` port. The spec's screen-width-based quadratic sizing and Vector3 width/height×Z were dismantled in BD's PBR sync — same donor-code-gone pattern as A1.3/A5.7. No port; grep for "quadratic" in BD newview turns up only lighting attenuation) | — |
| A5.1 tonemappers | **closed — superseded by Alchemy's own effects stack** (`RenderTonemapType` + gCGTonemap* program family in colorCorrect; selectable tonemapping native) | — |
| A5.2 HDR/auto-exposure | **closed — superseded** (Alchemy-native HDR path: `generateBloomHDR` + eye adaptation in `colorCorrect`, renderFinalize hdr branch) | — |
| A5.3 chromatic aberration | **closed — already present** (`applyChromaticAberration` in Alchemy's postEffectUtilsF/colorCorrectF effects stack) | — |
| A5.4 motion blur | open — **scoped 2026-07-10: SUBSYSTEM port, next big Fable block.** Donor ALIVE in BD (gDeferredMotionBlurProgram + renderMotionBlurComposite in renderFinalize + motionBlur[FV].glsl). The cost is the velocity buffer: `renderGeomMotionBlur` runs a per-object velocity geometry pass via virtual hooks on EVERY draw pool (getNumMotionBlurPasses/renderMotionBlur), requiring previous-frame matrix history incl. rigged skinning palettes. Multi-session; do not attempt as a tail-end item | — |
| A5.5 sepia/greyscale/posterize/CAS | **closed — superseded** (CAS native `applyCAS`; artistic looks via Alchemy's LUT color-grading system in the Lightbox — strictly more capable than BD's fixed filters) | — |
| A5.6 light-source toggles | **done** | — |
| A5.7 high-altitude shadows | **closed — donor code gone** (BD d0f2fbe499 made shadow_bias a per-cascade Vector4; current BD reverted to scalar in the PBR sync. If altitude shimmer persists after G4.1 per-cascade resolution, reimplement the vec4 bias novel — contained: uniform type + 4-way setting + shadowUtil sampling sites) | — |
| B1 machinima sidebar | **done — recast** (`90fab9497e`: BD's sidebar architecture deliberately not ported; the Lightbox is this fork's surface. Avatar tab surfaces A5.6 light classes, B4 limits, B5 thresholds, B12 head scale; Machinima/Godrays/Shadows tabs cover the rest. Every merged runtime setting now has UI) | — |
| B2 camera presets | **done** | — |
| B3 freeze world | **done** | — |
| B3a animation speed | **done** (`333e1e57b4`) | — |
| B4 head/eye tracking limits | **done** | — |
| B5 max cam/pelvis diff | **closed — already present via LL upstream** (2026-07-09: `AvatarRotateThresholdSlow`/`Fast` debug settings wired at `llvoavatar.cpp:4613` — same change BD carries as LL commit 9fe788e031; Ground Rule 3, no double-apply. Slider UI deferred to B1 sidebar, which should surface these two settings) | — |
| B6 bone camera | **done** (100% donor-behavior port per user directive; see commit for donor refs. llcinematiccamera MODE_BONE_LOCK retained as the separate hard-lock tool. **2026-07-11 source re-verification (B8s): BD's `CameraFollowJoint` is focus-follow, NOT a position hard-lock — the B6 port already matches BD byte-for-byte, so no change; the "hard lock" directive premise was retired**) | — |
| B7 poser | **closed — already present via upstream** (2026-07-10: Alchemy adopted Firestorm's Poser upstream — `fsfloaterposer.cpp`/`fsposeranimator.cpp` with active upstream maintenance, World-menu entry `fs_poser`; resolves the F10 FS-vs-BD donor decision in favor of FS by upstream fiat; in-world capability verification owed) | — |
| B8 cinematic camera mode | **done** (BD Cinematic Head Tracking ported verbatim; bone camera precedence per donor; Lightbox Avatar tab UI. Our llcinematiccamera motion patterns remain as a separate, additive tool. **Verbatim-BD fidelity corrected by B8s (2026-07-11): dropped the non-BD up-vector normalize, restored BD's roll-reset + if/else-if nesting, `CameraMaxRoll*` → BD 0.85, all behind default-off `BDMergeCameraStraightLift`**) | — |
| B9 gamepad flycam | open — next-session candidate (PR-3: BD joystick input authoritative; BD's flycam input mapping/deadzone/smoothing must COMPOSE with our `llcameraoperator` procedural handheld layer, not replace it) | — |
| B10 pose sync | open | B7 |
| B11 lookAt broadcast off | **done** (limit-distance half was already present) | — |
| B12 mouselook head scaling | **done** (experimental. Camera-behaviour region reconciled to verbatim BD by B8s (2026-07-11); the head-bone scaling itself is unchanged) | — |
| B13 EEP editor rework | **done** (`bf3a94f798`; scope shrank on recon — BD's resurrected classic editors are dead in BD HEAD; ported the live subset: preset library + water adjust + env settings window + gated local-preset surface + toolbar buttons. In-world smoke test owed) | — |
| C1 client AO | open | — |
| C2 movement conveniences | **done** (partial port — 2 of 3 sub-features already present) | — |
| C3 camera QoL | **done** (2 of 3 sub-features already present) | — |
| C4 click-to-walk hardening | **closed — already present via shared upstream** (2026-07-09: fly-click-walk early-return `lltoolpie.cpp:632` + `stopClickToWalk()` from `toggleFlying()`; HUD double-click guards `lltoolpie.cpp:660,754` + `mDoubleClickTimer`; alpha/particle immune via `pick_transparent=false`/`pick_particle=false` at `lltoolpie.cpp:597,607,742`. BD's fixes were 2011–2021 upstream LL commits both trees inherited; verified in current source, not just history) | — |
| C5 rebindable keys | **closed — no port** (PR-1; verified 2026-07-09: `llkeyconflict.cpp/h` + `key_bindings.xml` present, wired into preferences) | — |
| C6 snapshot conveniences | **done** | — |
| C7 texture channel lock | **closed — already present via shared upstream** (2026-07-09: "Synchronize materials" checkbox, `SyncMaterialSettings` + `syncOffsetX/Y`/`syncRepeatX/Y`/`syncMaterialRot` in `llpanelface.cpp`, upstream MAINT-3223 commit `69db7cd947` carried identically by BD and Alchemy; PBR/GLTF transforms untouched by sync in both trees, matching BD scope) | — |
| C8 slider precision | **done** | — |
| F1 area search | **closed — already present** (2026-07-10 sweep, deep-verified after user challenge: Scene Explorer searches name/description/owner/UUID with region/parcel/radius scope (`alsceneexplorerpredicate.h`) AND actively fetches all region objects' properties from the sim via a batched priority-queue pump (`alfloatersceneexplorer.cpp:1498` region) — the FS-defining mechanism; full action set in `menu_scene_explorer.xml`. Recent upstream work (2026, Rye Mutt) — absent from older Alchemy builds) | — |
| F2 derender + asset blacklist | **closed — already present** (`alderenderlist.cpp` + `alassetblocklist.cpp` with LLSD save/load persistence + `panel_al_block_derender.xml` management UI) | — |
| F5 camera roll | **closed — already present** (`LLAgentCamera::cameraRollOver`/`resetCameraRoll`, bindable `roll_left`/`roll_right` actions, default Shift+Q/E in `key_bindings.xml`; precise-positioning half covered by stock camera floater + C3/C8 spinners) | — |
| C1 client AO | **closed — already present** (full AO: `ao.cpp`/`aoengine.cpp`/`aoset.cpp` + `floater_ao.xml`/`panel_ao*.xml`) | — |
| F7 sound + animation explorer | **half-closed**: sound explorer present (`floater_explore_sounds.xml`); animation explorer absent — port briefed (B-2b.7) | — |
| F3 extended radar + show friends only | **done** (`c80b457101`; see landed table. In-world smoke test owed; radarbox_* icon assets follow-up) | — |
| F4/F8 phototools + composition guides | **done** (`f33b3af015`; recast thin — see landed table. In-world verification owed: WYSIWYG DoF vs snapshot, guides excluded from captures) | — |
| F6 movelock / pose stand / undeform | **split ruling (2026-07-10)**: movelock is NOT portable standalone — FS implements it through the FS LSL bridge (`fslslbridge.cpp`, worn scripted attachment doing the actual position lock; a pure client cannot resist pushes) → gated on a future whole-bridge decision. Pose stand + undeform are client-side and portable — briefed (B-2b.8) | movelock: LSL-bridge decision |
| G5.0 memory prerequisites & fast wins | **done** (in-world validation owed: memory readout >16GB, VRAM detect, capture degrade check) | — |
| G5.1 decoded-texture RAM pool | Stage 1 spike + Stage 2 core + **mesh pool (6d6fdf169d)** + **decode ceiling (1557a892fa)** landed. Remaining Stage 2b briefed for implementer models in `doc/BDMERGE_G5_BRIEFS.md`: tiers (B-2b.1), per-discard entries (B-2b.5), AVX-512 experiment (B-2b.4) | G5.0 ✓ |
| G5.2 texture pinning / capture mode | **landed** (`7c89378321`, `BDMergeCaptureModePin`, default off) | — |
| G5.3 scene warm-up / preload | **closed — descoped (user, 2026-07-11)**: marginal convenience; G5.1's session-resident texture/mesh pools already largely eliminate the first-seconds streaming this targeted. Brief B-2b.2 retired; the warm-up button in B-2b.3 is dropped with it. | — |

*(G5.x ids are from the 2026-07 top-tier spec sheet — "Alchemy Fork Spec Sheet
BD Graphics + FS QoL", memory/streaming phase for the RTX 5090 / 192GB target;
no A/B/C equivalent existed. G5.0 findings map to the companion "Alchemy Memory
Analysis" doc, findings 1/3/4/6.)*

### Target-hardware notes for G5.1 Stage 2 (recorded 2026-07-10)

Machine: **AMD Ryzen 9 9950X** (16 cores / 32 threads, Zen 5, full AVX-512) +
RTX 5090 32GB + 192GB RAM. CPU-specific levers for the threading sub-item
(spec G5.1 4a):

- `image_decode_count = llclamp(cores − 6, 2, 16)` (`llappviewer.cpp:2189-2233`):
  with 32 logical cores the formula gives 26 but the **16-thread clamp binds** —
  the ceiling is the lever here, not the formula. Retune per the spike's queuing
  data (spec 4a-i). Verify whether `cores` reads logical (32) or physical (16)
  before retuning.
- 9950X is homogeneous (no P/E hybrid) — the spec's anti-pinning caveat is moot
  on this box, but worker-pool isolation (4a-ii) remains the right design anyway.
- **AVX-512 J2K decode**: Zen 5 has the full 512-bit datapath and the viewer's
  decoder is OpenJPEG 2.5.4. Personal build, no distribution → free wins
  available from compiler arch flags (`/arch:AVX512` on the alchemy-bin +
  openjpeg vcpkg port) and/or evaluating an SIMD-optimized J2K path. Only worth
  pursuing if the spike confirms decode-dominance; file under G5.1 Stage 2.
